* `make install-ruby`: to install ruby.
* `bundle install`: to install plugins.
* `bundle exec jekyll serve`: to serve.

* sudo nginx -s reload && sudo service nginx restart && && sudo rm -rf  /var/cache/nginx
