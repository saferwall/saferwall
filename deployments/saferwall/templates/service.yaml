apiVersion: v1
kind: Service
metadata:
  name: {{ include "saferwall.fullname" . }}
  labels:
    {{- include "saferwall.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "saferwall.selectorLabels" . | nindent 4 }}
