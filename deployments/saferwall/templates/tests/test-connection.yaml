apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "saferwall.fullname" . }}-test-connection"
  labels:
{{ include "saferwall.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "saferwall.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
